extends ../layout

block page_includes
    script(src="/dist/system.js")



    script.
        //used to tell the bundled js which modules to load.
        window.jsmodule = {
            module: "system",
            loaders: ["system.logs"],
        };
        window.dockerArgs.activeContainer = "systemContainer";
        window.dockerArgs.activeElementId = "systemLogs";


block content
    #wrapper
        #page-container.page-container
            #page-container-inner
                section#sec1
                    #sec-content.subpage-container
                        #content1-heading.sec-heading.subpage-heading
                            h3 activity logs
                        #content1-text.sec-content
                            .subpage-options
                            .subpage-content
                                #loglist-container.loglist-container




