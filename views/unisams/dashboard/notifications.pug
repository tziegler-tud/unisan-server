extends ../layout

block page_includes
    script(src="/dist/dashboard.js")

    script.
        window.jsmodule = {
            module: "dashboard",
            loaders: ["notifications"],
        };
        window.dockerArgs.activeContainer =  "dashboardContainer";
        window.dockerArgs.activeElementId = "dashboardNotifications";


block content
    #wrapper
        #page-container.page-container
            #page-container-inner.page-container-inner.full-width.full-height
                section#sec1
                    #sec-content.subpage-container
                        #content1-text.sec-content
                            .subpage-content
                                .dashinfo-container
                                    #dashPage-component-container.dashPage-component-container

                section#sec2