extends ./settings

block page_includes

    script.
        window.jsmodule = {
            module: "settings",
            loaders: ["settings.mail"],
        };
        window.dockerArgs.activeContainer = "personalSettingsContainer";
        window.dockerArgs.activeElementId = "personalSettings_mail";
        window.allowedit = #{allowedit};

    //link(rel='stylesheet', href='/static/unisams/stylesheets/lssc_loader_userprofile.css')
    //link(rel='stylesheet', href='/static/unisams/stylesheets/editUser.css')

    script(src="/dist/settings.js")

