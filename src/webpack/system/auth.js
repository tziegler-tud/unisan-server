
let auth = {
    init: function () {
        $(document).ready(function () {

            //debug line, remove before flight
            console.log("loading js module: system.auth");

        })
    }
};

export {auth}